export function getTriangleType(a, b, c) {
    if (!Number.isInteger(a) 
        || !Number.isInteger(b) || !Number.isInteger(c)) {
            throw new Error("Invalid params")
        }

    let tipoAngulo = "Obtuso"
    return "Obtuso"

}